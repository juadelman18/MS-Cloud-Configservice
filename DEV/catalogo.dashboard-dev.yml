# HTTP Server
server:
  port: 5555   # HTTP (Tomcat) port
  
eureka:
  client:
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:2222/eureka

turbine:
  aggregator:
    clusterConfig: ms.cesta
  appConfig: ms.cesta
  amqp:
    port: 5672